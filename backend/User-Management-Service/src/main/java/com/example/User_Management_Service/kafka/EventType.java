package com.example.User_Management_Service.kafka;

public enum EventType {
    USER_REGISTERED,
    USER_DELETED,
    USER_STATUS_CHANGED,
    WORKER_PROFILE_SAVED,
    PROJECT_PROVIDER_PROFILE_SAVED
}