# application.properties IS committed (uses ${ENV:} placeholders — no real secrets).
# Only the local-only secret overrides that fill those placeholders are ignored:
**/src/main/resources/application-local.properties
**/src/main/resources/application-secrets.properties

# Java / Maven / Spring Boot
target/
*.class
*.jar
*.war
*.ear
.mvn/wrapper/maven-wrapper.jar

# IDE
.idea/
*.iml
.vscode/
*.swp

# Node / Frontend
node_modules/
dist/
build/
.env
.env.*

# OS
.DS_Store
Thumbs.db
